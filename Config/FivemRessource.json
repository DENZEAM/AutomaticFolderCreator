{
  "folder": {
    "Preset": {
      "Name": "FiveM Resource",
      "Days": "False",
      "underFolder": ["client", "server", "shared", "assets"],
      "underUnderFolder": {
        "assets": ["images", "sounds"]
      },
      "underFilesFolder": {
        "client": ["client.lua"],
        "server": ["server.lua"],
        "shared": ["config.lua"]
      }
    }
  }
}
